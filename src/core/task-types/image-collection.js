async function patchTaskData(task, params, global) {}
async function getTaskData(task, params, global) {}

async function createAssignment(task, params, global) {}

async function patchAssignmentData(assignment, params, global) {}
async function getAssignmentData(assignment, params, global) {}

module.exports = {
  patchTaskData,
  getTaskData,
  createAssignment,
  patchAssignmentData,
  getAssignmentData
};
